# Receive an integer number from the user via keyboard input
num = int(input('Enter a number: '))

# Analyze the number and display its predecessor and sucessor
print('Analyzing the number {}, its predecessor is {} and its sucessor is {}'.format(num, (num - 1), (num + 1)))
